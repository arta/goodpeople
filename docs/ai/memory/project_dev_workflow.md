---
name: Dev workflow — bin/dev rebuilds CSS automatically
description: bin/dev watches and rebuilds CSS — no need to run npm run build:css manually
type: project
---
`bin/dev` watches and rebuilds CSS automatically — no need to manually run `npm run build:css` after file changes.
